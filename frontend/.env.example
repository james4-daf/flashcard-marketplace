# Base URL of the Hono API (Cloudflare Worker). Copied to `.env` by `npm install`.
# Leave empty in dev: requests are same-origin (/api) and proxied to the worker
# by the Vite dev server, so the app works through a single (forwarded) port.
# In production, set this to the deployed Worker URL.
VITE_API_URL=""
